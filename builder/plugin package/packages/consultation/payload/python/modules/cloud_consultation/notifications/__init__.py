"""In-app consultation notifications.

Qt-free core (``models``, ``inbox``, ``detect``) is importable and testable without
PySide6. The Qt pieces (``service``, ``poller``) are imported on demand by the UI.
"""
