"""Shared constants for the AI-PACS Lite Viewer.

Kept in a dedicated module (not ``__init__``) so both package-relative and
standalone (Nuitka --standalone script) imports resolve identically.
"""

VIEWER_VERSION = "1.3.0"
VIEWER_DISPLAY_NAME = "AI-PACS Lite Viewer"
VIEWER_EXE_NAME = "AIPacsLiteViewer.exe"
