# Build toolchain (pin and validate together in .venv_build)
PyInstaller==6.11.1
pyinstaller-hooks-contrib==2025.0
setuptools>=68
wheel>=0.41
packaging>=23

# Runtime packages that must be present in the build venv for PyInstaller
# to trace hidden imports correctly and bundle the DLLs/extensions.
# Keep in sync with imports_summary.json suggested_hiddenimports.
psutil>=5.9

