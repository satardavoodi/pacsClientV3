"""DX wrist intelligent AI analysis.

This package is deliberately separate from mammography analysis and from the
native bone-age inference model. It converts wrist radiographs to PNG and
sends them directly to the configured multimodal language-model API.
"""
