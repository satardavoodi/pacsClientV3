"""Phase 6 UI — account popup, consultation compose dialog, and inbox.

Imported lazily (only when the account pill is clicked / a consultation action runs),
so PySide6 is not required to import the rest of the module.
"""
