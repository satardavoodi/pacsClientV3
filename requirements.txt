PySide6==6.10.2
pynetdicom==2.1.1
pydicom>=2.4.0
pylibjpeg>=2.0.0
pylibjpeg-libjpeg>=2.3.0
pylibjpeg-openjpeg>=2.4.0
pylibjpeg-rle>=2.2.0
grpcio
google==3.0.0
google-api-python-client==2.168.0
vtk
SimpleITK==2.5.3
opencv-python-headless
natsort==8.4.0
sounddevice==0.5.2
soundfile==0.13.1
qasync
numpy
QtAwesome
pandas
openai==1.97.0
requests[socks]>=2.31.0
python-dotenv
comtypes>=1.3.0
SpeechRecognition>=3.10.0
pyaudio
webrtcvad

# ── External Identity module (Google OAuth + secure token storage) ──
# Additive, feature-flagged (AIPACS_IDENTITY_MODULE / config). google-auth is
# already pulled in by google-api-python-client; pinned here for clarity.
google-auth>=2.29.0
google-auth-oauthlib>=1.2.0
keyring>=24.0.0
cryptography>=42.0.0

# ── Secretary background agent: education content search + OCR hook ──
# (2026-06-11) Pure-Python extractors for full-text search inside
# educational PDFs / e-books / PowerPoint decks. pytesseract is the OCR
# BINDING only — the Tesseract engine itself ships separately (bundled
# under tools/vendor/tesseract/ or via AIPACS_TESSERACT env / PATH);
# without the engine, OCR features stay silently disabled.
pypdf>=4.0.0
python-pptx>=0.6.23
pytesseract>=0.3.10

# ── Agent Gateway: mobile / MCP connectivity (2026-07-17) ──
# segno is a ZERO-dependency pure-Python QR generator (no Pillow needed) used to
# render the pairing QR in Settings ▸ Agent. Feature is default-OFF; the gateway
# reuses cryptography (self-signed TLS) + requests (relay tunnel), both already
# pinned above. If segno is absent the tab falls back to showing the pairing URI
# as text, so this dependency is soft.
segno>=1.5.2
# Persistent outbound WebSocket to the AIPACS relay (remote access with no static
# IP / no port forwarding). SOFT dependency: if absent the gateway falls back to
# the long-poll relay transport, so the build still works without it.
websocket-client>=1.7.0
