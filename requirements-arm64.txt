# AI-PACS — Windows ARM64 (win_arm64) requirements  [ARM64 plan §7.1, 2026-07-07]
#
# Install ONLY on an ARM64 Windows machine with an ARM64 CPython (3.11+; 3.13
# recommended) — see tools/build/setup_arm64_env.ps1. This file is the
# "arm64-lite" (Phase 1) set: the FAST 2D domain, which is VTK-free by
# architecture. VTK / SimpleITK enter in Phase 2/3 as locally-built wheels.
#
# Differences vs requirements.txt (x64):
#   - PySide6 >= 6.11.1  (first release with official win_arm64 wheels)
#   - grpcio DROPPED     (transport retired per CLAUDE.md — dead code on x64 too)
#   - vtk / SimpleITK    EXCLUDED (no win_arm64 wheels on PyPI — Phase 2/3 source builds)
#   - unverified-wheel packages moved to the OPTIONAL section below
#     (setup_arm64_env.ps1 installs them best-effort, one by one)

# ── Core (verified or pure-Python) ──────────────────────────────────────────
PySide6>=6.11.1
pynetdicom==2.1.1
pydicom>=2.4.0
numpy
pandas
natsort==8.4.0
qasync
QtAwesome
requests[socks]>=2.31.0
python-dotenv
comtypes>=1.3.0
openai==1.97.0
google==3.0.0
google-api-python-client==2.168.0
google-auth>=2.29.0
google-auth-oauthlib>=1.2.0
keyring>=24.0.0
cryptography>=42.0.0
pypdf>=4.0.0
python-pptx>=0.6.23
pytesseract>=0.3.10
SpeechRecognition>=3.10.0
psutil

# ── OPTIONAL (wheel availability on win_arm64 unverified — best-effort) ─────
# Compressed-DICOM decode plugins. REQUIRED in practice if servers send
# compressed transfer syntaxes; if pip fails here, build from source or use
# https://github.com/cgohlke/win_arm64-wheels and re-verify decode tests.
#OPTIONAL pylibjpeg>=2.0.0
#OPTIONAL pylibjpeg-libjpeg>=2.3.0
#OPTIONAL pylibjpeg-openjpeg>=2.4.0
#OPTIONAL pylibjpeg-rle>=2.2.0
# Case-media video encode + pooyan filter (feature degrades gracefully):
#OPTIONAL opencv-python-headless
# Voice recording / playback stack (voice features disabled when absent):
#OPTIONAL sounddevice==0.5.2
#OPTIONAL soundfile==0.13.1
#OPTIONAL pyaudio
#OPTIONAL webrtcvad

# ── Phase 2/3 (locally built wheels, installed from the ARM64 wheelhouse) ───
# vtk        -> build via https://github.com/KitwareMedical/VTKPythonPackage
#               (MSVC ARM64 toolchain; modules: RenderingOpenGL2,
#               RenderingVolumeOpenGL2, ImagingReslice, Widgets, GUISupportQt)
# SimpleITK  -> build via https://github.com/SimpleITK/SimpleITKPythonPackage
